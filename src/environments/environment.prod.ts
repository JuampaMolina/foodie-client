export const environment = {
  production: true,
  // Sustituir por la URL real del backend en Render tras desplegarlo
  // (Dashboard > foodie-server > la URL que aparece arriba, algo como
  // https://foodie-server-XXXX.onrender.com). Commitear el cambio para que
  // Vercel vuelva a compilar con la URL correcta.
  apiBaseUri: 'https://foodie-server.onrender.com',
};
